# 🐍 Python Keywords

## ✨ What are Keywords?

**Keywords** are reserved words in Python that have predefined meanings.  
They **cannot** be used as variable names or identifiers.

They define the **structure and logic** of a Python program and are:

- 🔒 Reserved
- ⚠️ Case-sensitive
- 🧠 Core part of Python syntax

---

## 🔹 Logical Operators

| Keyword | Description |
|----------|------------|
| `and` | Returns `True` if **both** conditions are true |
| `or` | Returns `True` if **at least one** condition is true |
| `not` | Reverses the truth value |

---

## 🔹 Conditional Statements

| Keyword | Description |
|----------|------------|
| `if` | Starts a conditional block |
| `else` | Executes when `if` condition is `False` |
| `elif` | Checks additional conditions |

---

## 🔹 Loops

| Keyword | Description |
|----------|------------|
| `while` | Repeats code while condition is `True` |
| `for` | Iterates over a sequence |
| `in` | Checks presence inside a sequence |

---

## 🔹 Exception Handling ⚠️

| Keyword | Description |
|----------|------------|
| `try` | Defines block to test for errors |
| `except` | Handles the exception |
| `finally` | Always executes after try/except |

---

## 🔹 Functions

| Keyword | Description |
|----------|------------|
| `def` | Defines a function |
| `return` | Sends back a value from function |
| `lambda` | Creates anonymous function |

---

## 🔹 Object-Oriented Programming (OOP)

| Keyword | Description |
|----------|------------|
| `class` | Defines a class |
| `is` | Checks object identity |

---

## 🔹 Modules & Imports

| Keyword | Description |
|----------|------------|
| `import` | Imports a module |
| `from` | Imports specific components |
| `as` | Creates an alias for module |

---

## 🔹 Special Constants

| Keyword | Description |
|-------
